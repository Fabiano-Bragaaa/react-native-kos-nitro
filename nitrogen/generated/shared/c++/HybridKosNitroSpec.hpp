///
/// HybridKosNitroSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `KOSAccount` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct KOSAccount; }
// Forward declaration of `WalletOptions` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct WalletOptions; }
// Forward declaration of `KOSTransaction` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct KOSTransaction; }
// Forward declaration of `EvmTransactionOptions` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct EvmTransactionOptions; }
// Forward declaration of `BtcTransactionOptions` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct BtcTransactionOptions; }
// Forward declaration of `SubstrateTransactionOptions` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct SubstrateTransactionOptions; }
// Forward declaration of `CosmosTransactionOptions` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct CosmosTransactionOptions; }
// Forward declaration of `BigNumber` to properly resolve imports.
namespace margelo::nitro::kosnitro { struct BigNumber; }

#include <string>
#include "KOSAccount.hpp"
#include "WalletOptions.hpp"
#include <optional>
#include "KOSTransaction.hpp"
#include "EvmTransactionOptions.hpp"
#include "BtcTransactionOptions.hpp"
#include "SubstrateTransactionOptions.hpp"
#include "CosmosTransactionOptions.hpp"
#include <variant>
#include <NitroModules/ArrayBuffer.hpp>
#include <vector>
#include "BigNumber.hpp"

namespace margelo::nitro::kosnitro {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `KosNitro`
   * Inherit this class to create instances of `HybridKosNitroSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridKosNitro: public HybridKosNitroSpec {
   * public:
   *   HybridKosNitro(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridKosNitroSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridKosNitroSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridKosNitroSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string generateMnemonic(double size) = 0;
      virtual bool validateMnemonic(const std::string& mnemonic) = 0;
      virtual std::string getPathByChain(double chainId, double index, bool useLegacyPath) = 0;
      virtual KOSAccount generateWalletFromMnemonic(const std::string& mnemonic, double chainId, double index, const std::optional<WalletOptions>& options) = 0;
      virtual KOSAccount generateWalletFromPrivateKey(double chainId, const std::string& privateKey, const std::optional<WalletOptions>& options) = 0;
      virtual KOSTransaction signTransaction(const KOSAccount& account, const std::string& raw, const std::optional<std::variant<EvmTransactionOptions, BtcTransactionOptions, SubstrateTransactionOptions, CosmosTransactionOptions>>& options) = 0;
      virtual std::shared_ptr<ArrayBuffer> signMessage(const KOSAccount& account, const std::string& hex, bool legacy) = 0;
      virtual bool isChainSupported(double chainId) = 0;
      virtual std::vector<double> getSupportedChains() = 0;
      virtual std::string encryptWithGcm(const std::string& data, const std::string& password, double iterations) = 0;
      virtual std::string encryptWithCbc(const std::string& data, const std::string& password, double iterations) = 0;
      virtual std::string encryptWithCfb(const std::string& data, const std::string& password, double iterations) = 0;
      virtual std::string decrypt(const std::string& data, const std::string& password, double iterations) = 0;
      virtual BigNumber bigNumberNew(const std::string& value) = 0;
      virtual std::string bigNumberString(const BigNumber& value) = 0;
      virtual BigNumber bigNumberAdd(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual BigNumber bigNumberSubtract(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual BigNumber bigNumberMultiply(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual BigNumber bigNumberDivide(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual BigNumber bigNumberPow(const BigNumber& base, const BigNumber& exponent) = 0;
      virtual bool bigNumberIsEqual(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual bool bigNumberIsGt(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual bool bigNumberIsGte(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual bool bigNumberIsLt(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual bool bigNumberIsLte(const BigNumber& lhs, const BigNumber& rhs) = 0;
      virtual bool bigNumberIsZero(const BigNumber& value) = 0;
      virtual bool bigNumberIsPositive(const BigNumber& value) = 0;
      virtual bool bigNumberIsNegative(const BigNumber& value) = 0;
      virtual std::shared_ptr<ArrayBuffer> generateXpub(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index) = 0;
      virtual std::string getXpubAsString(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index) = 0;
      virtual std::shared_ptr<ArrayBuffer> deriveXpub(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index, const std::string& derivationPath) = 0;
      virtual std::shared_ptr<ArrayBuffer> slip77MasterBlindingKey(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index) = 0;
      virtual std::shared_ptr<ArrayBuffer> signEcdsaRecoverable(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index, const std::string& msgHex) = 0;
      virtual std::shared_ptr<ArrayBuffer> hmacSha256(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index, const std::string& derivationPath, const std::string& msgHex) = 0;
      virtual std::shared_ptr<ArrayBuffer> eciesEncrypt(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index, const std::string& msgHex) = 0;
      virtual std::shared_ptr<ArrayBuffer> eciesDecrypt(const std::string& mnemonic, const std::string& passphrase, bool isMainnet, double index, const std::string& msgHex) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "KosNitro";
  };

} // namespace margelo::nitro::kosnitro
